<div class="mb-3">
<input type="{{ $type }}" name="{{ $name }}" value="{{ $value}}" class="form-control" id="{{ $id }}">
<label for="{{ $name }}" class="form-label">{{ $label }}</label>
<select for="{{ $name }}" name="{{ $name }}" class="form-control" id="{{ $ id }}">
    <option value="{{ $value }}"> <!-- essa option fecha aqui ou no create.blade? -->

</div>
