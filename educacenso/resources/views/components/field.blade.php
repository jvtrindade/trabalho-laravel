<div class="mb-3">
    <label for="{{ $id }}" class="form-label">{{ $label }}</label>
    <input type="{{ $type }}" name="{{ $name }}" value="{{ $value }}" class="{{ $class }}" id="{{ $id }}" onclick="{{ $onclick }}">
</div>
